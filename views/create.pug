extends layout.pug
block content
    .errors-container
    form(class="create-form")
        .form-group
            label(for='name') Task name
            input#name.form-contro(type-'text', name="name", placeholder='Task name')
        button.tn.btn-primary(type='submit') Create Task
    
    script(src="js/create.js")